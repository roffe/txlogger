package main

func runFileChild() {
	// This is a no-op on Windows and will never be called because the FP environment variable is only set in the Linux build, but we need it to exist to satisfy the linker.
}
